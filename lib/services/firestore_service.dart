/// Firestore CRUD service for player data.
///
/// TODO: Implement when Firebase is configured.
/// Will handle reading/writing player profiles, farm state,
/// financial records, and leaderboard data.
class FirestoreService {
  // Future<Player> getPlayer(String uid) async { ... }
  // Future<void> savePlayer(Player player) async { ... }
  // Future<void> saveFarmState(String uid, List<List<Tile>> grid) async { ... }
  // Future<void> addTransaction(String uid, Transaction tx) async { ... }
  // Stream<List<LeaderboardEntry>> getLeaderboard(String country) => ...;
}
